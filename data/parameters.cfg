#-----------------------------------------------------
[Application parameters]
#-----------------------------------------------------

CalibrationsDirectory = 'G:/Projects/BWS_Calibrations/Calibrations/'

#-----------------------------------------------------
[Geometry]
#------------------- Values in millimeters ------------

stages_position_at_tank_center =  44                  # Where is the tank centre in mm wrt mobile stages coords.

# New Bench Configuraiton
#psb_scanner_offset = 59.3

# Old Bench Configuration
#psb_scanner_offset = 44
#ps_scanner_offset = 62.5
#sps_scanner_offset = 0

#-----------------------------------------------------
[LabView output]
#-----------------------------------------------------

# Naming #
data_SA = ['DISC PH. HOME dir IN', 'DISC PH. HOME dir HOME']
data_SB = ['DISC PH. IN dir IN', 'DISC PH. IN dir HOME']
data_PD = ['WIRE PH. dir IN', 'WIRE PH. dir HOME']
automatic_ranging = 'Data Description start-stop windows 1 and 2'

#-----------------------------------------------------
[OPS processing parameters]
#-------------------------------- Time in seconds ----

# OPS #
sampling_frequency = 20e6
prominence = 0.1
slits_per_turn = 29374                                       # Disk with: 40um pitch -->14700,  20um Pitch --> 29374
relative_distance_correction_prameters = [1.48, 2.4, 3.4]    # Thresshold for position loss compensation
camelback_threshold = 0.0
REMP_reference_threshold = 0.2
References_Timming = [7, 362.0]                           # Time cose to where references are.[40.0, 372.0]
Process_Occlusions = True
Compensate_Eccentricity = False

# Filters #
OPS_processing_filter_freq = 1e6                            # Cutting LPF Frequencies on Hz for OPS and Laser
peaks_detection_filter_freq = 50e3

# Other #
tdms_minimum_size = 14000000                                # Safety check for corrupted files

# Analysis window (in percentage)#                          # Allows to restrict amount of data for fit and analysis
positions_for_fit = [0, 100]
positions_for_analysis = [0, 100]

# Fatigue test #
fatigue_test = no

